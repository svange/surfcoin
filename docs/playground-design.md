# The Shaping Bay — pump.fun API playground

`/playground` on the surf site: sign in with Cognito, link your pump.fun
wallet, then poke every knob the pump.fun API exposes from a safe cockpit.

## Architecture

```
Browser SPA (/playground, code-split)
  │  Cognito managed login (OAuth code + PKCE)        template.yaml
  │  Bearer access-token on every call                stack: surfcoin
  ▼
HTTP API (JWT authorizer) ──► Lambda surfcoin-api (backend/)
                                │
                                ├─ DynamoDB surfcoin-data  (single table)
                                ├─ KMS alias/surfcoin      (Lightning key at rest)
                                ├─ Cognito admin APIs      (user approval / roles)
                                ├─ pump.fun frontend API   (data, proxied — CORS/bot walls)
                                ├─ PumpPortal trade APIs   (Lightning + Local build)
                                └─ Solana JSON-RPC         (balances, tx submit)
EventBridge rate(1 minute) ──► same Lambda ("autopilot-tick") — rule engine
```

- **Single stack** `surfcoin` (`template.yaml`): S3+CloudFront site serving
  `surfcoin.fail` plus the whole playground backend.
- **Contract**: `shared/types.ts` — the browser only ever sees these shapes.
- **Config**: `src/playground/runtime.ts`, generated by
  `scripts/sync-playground-config.sh` from stack outputs.
- **Deploys**: gitops — `.github/workflows/deploy.yaml` runs `sam deploy`,
  regenerates `runtime.ts`, then `scripts/publish-frontend.sh`.

## Security stance

- pump.fun has **no OAuth/SSO**. "Linking" = proving wallet ownership by
  signing a server-issued nonce (SIWS-style); manual trades can be signed in
  the browser wallet so private keys never leave it. Automation uses a
  PumpPortal Lightning API key: stored KMS-encrypted, **write-only** (never
  returned to the browser), used server-side only.
- **`liveTrading: false` by default** — the master safety. Manual Lightning
  trades, fee claims, and autopilot rules are forced to dry-run, and
  wallet-signed `trade/build` + `trade/submit` are refused outright (they have
  no dry-run form), until the user flips the setting.
  `autopilotEnabled: false` by default too; the tick is a no-op until armed.
- All `/api/*` routes sit behind the Cognito JWT authorizer; per-user data is
  keyed by the token's `sub`, so users can only reach their own rows.
- **Roles** live in Cognito user-pool groups (`approved`, `admins`), read from
  the `cognito:groups` claim in the Lambda (the authorizer only validates the
  token; it doesn't check groups). New signups have no group = pending: they see
  only an approval-pending page and every `/api` call 403s
  (`code: PENDING_APPROVAL`) until an admin approves them from the Users tab.
  The decision lives in one place — `backend/src/rbac.ts` — and is unit-tested
  (`rbac.test.ts`, under the enforced coverage gate) so the gate can't silently
  regress. The gate is enforced on **both** capability paths: the synchronous
  `/api` handler and the async autopilot tick (below), so revoking a user stops
  their autopilot too, not just their API access. `GET /public/*` (the `/coins`
  page) is unauthenticated.

## DynamoDB layout (single table, pk/sk)

| pk           | sk              | item                                      |
|--------------|-----------------|-------------------------------------------|
| USER#\<sub\> | PROFILE         | wallet, KMS-ciphertext PumpPortal key, settings |
| USER#\<sub\> | NONCE#\<nonce\> | wallet-link nonce (TTL via `expiresAt`)   |
| USER#\<sub\> | RULE#\<id\>     | autopilot rule                            |
| USER#\<sub\> | ACT#\<ts\>#\<id\>| activity log entry (trades + rule fires) |
| SITE         | REGISTRY        | tracked-coins registry (ownerSub, creatorWallet, mints) |

The autopilot tick finds work with a deliberate table Scan for enabled rules
(tiny table) — there is no per-user marker row. Before evaluating anything it
filters those rules to owners currently in the `approved`/`admins` groups
(`filterApprovedOwners`), and fails closed if that membership lookup errors, so
a revoked user's rules stop firing.

## API routes (all JWT, under /api)

Account: `GET /me` · `PUT /settings` · `GET /wallet/nonce` ·
`POST /wallet/link` · `DELETE /wallet/link` · `PUT /pumpportal` ·
`DELETE /pumpportal`
Registry: `GET/PUT /registry` (tracked coins; first writer claims ownership)
Data: `GET /portfolio` · `GET /coins/created` · `GET /coin/{mint}` ·
`GET /coin/{mint}/candles` · `GET /coin/{mint}/trades` · `GET /explore` ·
`GET /pump/raw?path=…` (allowlisted passthrough for the API-explorer panel)
Trading: `POST /trade` (Lightning, `dryRun` default true) ·
`POST /trade/build` (unsigned tx for browser signing) · `POST /trade/submit` ·
`POST /claim-fees`
Autopilot: `GET/POST /rules` · `PUT/DELETE /rules/{id}` · `GET /activity`
Admin (requires `admins` group): `GET /admin/users` ·
`POST /admin/users/{sub}/groups`
Public (no auth, under /public): `GET /public/coins` (drives `/coins`)

## Frontend (`src/playground/`)

`PlaygroundPage` gates on auth state: signed-out → sign-in CTA; signed-in but
unapproved → pending-approval page (polls for the `approved` claim); approved →
`Dashboard` with tabs: Trade desk (`CoinsPanel` → `CoinDetail` with
`CandleChart` + `TradePanel`), Autopilot (`AutopilotPanel` + `ActivityPanel`),
API explorer (`RawExplorer`), Connections (`LinkPanel`, `ClaimFeesPanel`,
`TrackedCoinsPanel`), and an admin-only Users tab (`AdminUsersPanel`).

Chart palette is dataviz-validated against the `night` surface (#0B3038):
candle-up `#2EA189`, candle-down `#DC5240`, volume `#C48122`. Marks stay
thin, text wears text tokens (salt/seafoam), grid is hairline, crosshair +
single tooltip, no dual axis (price and volume are separate stacked panes).

## Voice

The playground is "The Shaping Bay" — where boards (and trades) get shaped.
Copy stays in the site's deadpan surf voice; danger states are honest and
loud ("live trading is ON — real SOL moves when you press this").
